version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'npm i'
        build:
            commands:
                - 'npm run build:dev'
    artifacts:
        baseDirectory: build
        files:
            - '**/*'
    cache:
        paths:
            - '.npm/**/*'